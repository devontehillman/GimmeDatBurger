<li>
	{{burger_name}}

	<button class="change-devoured" data-id="{{id}}" data-Devoured="{{devoured}}">
		{{#if devoured}}Undevour?{{else}}Devoured!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
